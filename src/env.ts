export const ENV = {
  LOCAL: "local",
  STAGE: "stage",
  SANDBOX: "sandbox",
  PRODUCTION: "production",
  TEST: "test",
} as const;
export const MOBILE_ENV = {
  ANDROID: "android",
  IOS: "iOS",
} as const;
